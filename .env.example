# Copy to .env.local and paste your key from https://aistudio.google.com/apikey
# Never commit .env.local. Vite exposes VITE_* vars in the browser — local/hackathon only.
VITE_GEMINI_API_KEY=AIzaSyBzh7D2mKyYNoyi8UEuan0Wv7zKoONrDeM

# Unset = frontend-only (no /api calls; no Vite proxy errors). Set `true` when the API is running:
#   cd backend && npm install && npm run dev   → http://localhost:3001
# VITE_USE_BACKEND_API=true
